# 🏗️ 시스템 설계서 (System Design Document)

> **프로젝트**: MyMoney - 독립 개인 가계부 & 자산 관리 시스템
> **기본 화폐**: VND (베트남 동) / KRW (대한민국 원)
> **문서 버전**: v2.0 (개인 가계부 단독 전용)
> **최종 수정일**: 2026-07-24

---

## BLOCK-00: 과제 격리 및 멀티태스크 관리 (Task Isolation)

### 0.1 프로젝트 정보
- 프로젝트 디렉토리: `g:\AI\05_Personal_Money`
- 개발 서버 포트: `5174`
- DB: Supabase (PostgreSQL)

---

## BLOCK-01: 메뉴 및 네비게이션 구조 (Navigation)

### 1.1 사이드바 & 하단 탭 메뉴 트리

```
📊 개인 대시보드 (Dashboard)         ← 자산 요약, 6개월 수입/지출 추이, 최근 거래
├── 💰 수입/지출 가계부 (Personal)   ← 수입/지출 기록, 내역 조회, 카테고리 관리
├── 💱 개인 환전 관리 (Exchange)    ← VND/KRW 환전 거래 기록 및 계산기
├── 📈 자산 분석 및 통계 (Analytics) ← 카테고리 지출 비율, 수입/지출 추이, 환전 현황
└── ⚙️ 설정 (Settings)             ← 테마, 데이터 백업/복원, DB 연결 테스트
```

### 1.2 라우팅 구조

| 경로 | 페이지 | 설명 |
|---|---|---|
| `/` | Dashboard | 개인 자산 요약 대시보드 |
| `/personal` | Personal Ledger | 개인 수입/지출 가계부 |
| `/exchange` | Exchange Ledger | 개인 환전 관리 |
| `/analytics` | Analytics | 자산 현황 및 통계 차트 |
| `/settings` | Settings | 설정 및 백업 |

---

## BLOCK-02: 개인 가계부 — Personal Ledger

### 2.1 주요 기능
- 수입/지출 내역 기록 (날짜, 구분, 카테고리, 금액(VND), 메모)
- 기간별 수입/지출 합계 및 누적 잔액 산출
- 사용자 지정 카테고리 (수입/지출 구분, 아이콘, 정렬)

### 2.2 DB 스키마 (Supabase)

```sql
-- 1. 카테고리 테이블
CREATE TABLE personal_categories (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name TEXT NOT NULL,
    type TEXT NOT NULL CHECK (type IN ('income', 'expense')),
    icon TEXT DEFAULT '💰',
    sort_order INT DEFAULT 0,
    is_active BOOLEAN DEFAULT true,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- 2. 수입/지출 거래 테이블
CREATE TABLE personal_transactions (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    tx_date DATE NOT NULL DEFAULT CURRENT_DATE,
    type TEXT NOT NULL CHECK (type IN ('income', 'expense')),
    category_id BIGINT REFERENCES personal_categories(id),
    amount NUMERIC NOT NULL CHECK (amount > 0),
    memo TEXT DEFAULT '',
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);
```

---

## BLOCK-04: 개인 환전 관리 — Exchange Ledger

### 4.1 주요 기능
- 베트남 동(VND)과 원화(KRW) 간 환전 기록
- 환율(1 KRW = ? VND), 환전 금액 자동 계산
- 순 VND 유출입 및 KRW 정산 관리

### 4.2 DB 스키마 (Supabase)

```sql
-- 3. 환전 거래 테이블
CREATE TABLE exchange_transactions (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    tx_date DATE NOT NULL DEFAULT CURRENT_DATE,
    person_name TEXT NOT NULL DEFAULT '본인',
    tx_type TEXT NOT NULL CHECK (tx_type IN ('VND_TO_KRW', 'KRW_TO_VND')),
    exchange_rate NUMERIC NOT NULL,
    amount_vnd NUMERIC NOT NULL CHECK (amount_vnd > 0),
    amount_krw NUMERIC NOT NULL CHECK (amount_krw > 0),
    memo TEXT DEFAULT '',
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);
```

---

## BLOCK-06: 앱 설정 — Settings

```sql
-- 4. 앱 설정 테이블
CREATE TABLE app_settings (
    key TEXT PRIMARY KEY,
    value JSONB NOT NULL,
    updated_at TIMESTAMPTZ DEFAULT NOW()
);
```
